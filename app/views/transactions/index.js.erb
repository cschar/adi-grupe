//similar to intercooler.js, send a big blob of html to replace element

$("#transactions-list").replaceWith("<%=j render partial: "list" %>");
console.log( "transaction index.js.erb")
addTransactionMarkers();